# =============================================================================
# CV Builder Backend Environment Variables
# Copy this file to .env and fill in the values
# =============================================================================

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:3000"

# Security - CORS Configuration
# Comma-separated list of allowed origins (leave empty to use FRONTEND_URL only)
# Example: "http://localhost:3000,https://yourdomain.com,https://www.yourdomain.com"
ALLOWED_ORIGINS=""

# Database
# Local: postgresql://postgres:postgres@localhost:5444/cv_builder?schema=public
# Neon:  postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/cv_builder?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5444/cv_builder?schema=public"

# JWT Authentication
# CRITICAL: Change these in production! Use strong, random values.
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-in-production-use-64-char-random-string"
JWT_EXPIRES_IN="7d"

# Session Secret (for future session-based auth)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-session-secret-change-in-production-use-32-char-random-string"

# =============================================================================
# AI Provider - OpenAI GPT-4o-mini (~125 resume tailoring runs per $1)
# Get API key at: https://platform.openai.com/api-keys
# =============================================================================

OPENAI_API_KEY="sk-your-openai-api-key"
OPENAI_MODEL="gpt-4o-mini"

# =============================================================================
# Cloudinary - File Storage (FREE 25GB)
# Get credentials at: https://cloudinary.com/console
# =============================================================================

CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# =============================================================================
# Job Search - Adzuna API (FREE)
# Get your free API keys at: https://developer.adzuna.com/
# =============================================================================

ADZUNA_APP_ID="your-adzuna-app-id"
ADZUNA_APP_KEY="your-adzuna-app-key"

# =============================================================================
# Security & Encryption
# =============================================================================

# Data Encryption Key (32 characters minimum)
# CRITICAL: Use a strong, unique key in production
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64').slice(0, 32))"
ENCRYPTION_KEY="your-32-character-encryption-key!"

# Rate Limiting (Optional - uses defaults if not set)
# RATE_LIMIT_WINDOW_MS=900000      # 15 minutes in milliseconds
# RATE_LIMIT_MAX_REQUESTS=100      # Max requests per window
# AUTH_RATE_LIMIT_MAX=5            # Max auth attempts per window
# AI_RATE_LIMIT_MAX=10             # Max AI requests per minute

# Performance Monitoring
# SLOW_QUERY_THRESHOLD=1000        # Log queries slower than this (in ms)

# =============================================================================
# Redis - Caching & Job Queue (FREE - local or Redis Cloud)
# For local development: Install Redis locally or use Docker
# For production: Use Redis Cloud (FREE 30MB) https://redis.com/try-free/
# =============================================================================

REDIS_HOST="localhost"
REDIS_PORT="6379"
# REDIS_PASSWORD=""  # Optional, for production use

# =============================================================================
# Email - SendGrid (FREE 100 emails/day)
# Get API key at: https://app.sendgrid.com/settings/api_keys
# =============================================================================

SENDGRID_API_KEY="SG.your-sendgrid-api-key"
EMAIL_FROM_ADDRESS="noreply@yourapp.com"
EMAIL_FROM_NAME="CV Builder"

# Optional: Gmail OAuth2 for sending emails
# GMAIL_CLIENT_ID=""
# GMAIL_CLIENT_SECRET=""
# GMAIL_REFRESH_TOKEN=""

# Optional: SMTP Configuration (alternative to SendGrid)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER=""
# SMTP_PASS=""

# =============================================================================
# OAuth Providers (FREE)
# =============================================================================

# Google OAuth - https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/auth/google/callback"

# GitHub OAuth - https://github.com/settings/developers
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"
GITHUB_REDIRECT_URI="http://localhost:3000/auth/github/callback"

# =============================================================================
# Sentry Error Monitoring (FREE 5K errors/month)
# Get DSN at: https://sentry.io/settings/projects/
# =============================================================================

SENTRY_DSN="https://your-key@sentry.io/your-project-id"
SENTRY_RELEASE="1.0.0"
